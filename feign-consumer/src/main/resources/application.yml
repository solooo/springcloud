spring:
  application:
    name: feign-consumer

server:
  port: 8084

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/

# ¶ÏÂ·Æ÷ hystrix
feign:
  hystrix:
    enabled: true